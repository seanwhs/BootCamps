# FastAPI Masterclass: Quiz & Test Bank

**Comprehensive Assessment Resource with Answer Keys**

---

## How to Use This Test Bank

This comprehensive assessment resource contains:

1. **Module Quizzes** - Quick checks after each module (10-15 questions each)
2. **Mid-Term Exam** - Comprehensive review after Module 3 (50 questions)
3. **Final Exam** - Complete assessment covering all modules (100 questions)
4. **Practical Coding Challenges** - Hands-on exercises with solutions
5. **Answer Keys** - Detailed explanations for all questions

---

## Module 1 Quiz: FastAPI Foundations

### Part A: Multiple Choice (1 point each)

**1. What does ASGI stand for?**
a) Asynchronous Server Gateway Interface
b) Application Server Gateway Interface
c) Async Standard Gateway Implementation
d) Advanced Server Gateway Interface

**2. Which of the following is NOT a feature of FastAPI?**
a) Automatic OpenAPI documentation
b) Built-in support for GraphQL
c) Data validation with Pydantic
d) Async/await support

**3. What HTTP method should be used to retrieve data without modifying it?**
a) POST
b) PUT
c) GET
d) DELETE

**4. How do you define a required field in Pydantic?**
a) `name: str = None`
b) `name: str = Field(default=None)`
c) `name: str = Field(...)`
d) `name: str = Field(default="")`

**5. Which status code indicates a resource was successfully created?**
a) 200 OK
b) 201 Created
c) 204 No Content
d) 302 Found

**6. What is the correct way to define a path parameter in FastAPI?**
a) `@app.get("/items/{item_id}")`
b) `@app.get("/items/:item_id")`
c) `@app.get("/items/<item_id>")`
d) `@app.get("/items/[item_id]")`

**7. What is the purpose of dependency injection in FastAPI?**
a) To make code faster
b) To improve testability and reusability
c) To automatically generate documentation
d) To handle database connections only

**8. Which decorator is used to handle POST requests?**
a) `@app.get`
b) `@app.post`
c) `@app.put`
d) `@app.delete`

**9. What is the correct way to raise an HTTP exception?**
a) `raise HTTPException(404, "Not found")`
b) `return HTTPException(404, "Not found")`
c) `raise HTTPException(status_code=404, detail="Not found")`
d) `HTTPException.raise(404, "Not found")`

**10. Which Pydantic field constraint would you use to ensure a string has at least 3 characters?**
a) `min=3`
b) `min_length=3`
c) `gt=3`
d) `ge=3`

### Part B: True/False (1 point each)

**11.** FastAPI automatically generates both Swagger UI and ReDoc documentation.
a) True
b) False

**12.** WSGI is the newer, faster alternative to ASGI.
a) True
b) False

**13.** Query parameters are always required in FastAPI endpoints.
a) True
b) False

**14.** The `@field_validator` decorator in Pydantic can validate multiple fields simultaneously.
a) True
b) False

**15.** FastAPI's dependency injection system can use classes, functions, and async functions.
a) True
b) False

### Part C: Fill in the Blank (2 points each)

**16.** The interactive API documentation generated by FastAPI is called _______________.

**17.** To create an optional query parameter, you need to provide a _______________.

**18.** The Pydantic field constraint for checking if a number is greater than zero is _______________.

**19.** The HTTP status code for "Not Found" is _______________.

**20.** In FastAPI, you import the `HTTPException` class from the _______________ module.

### Part D: Short Answer (5 points each)

**21.** Explain the difference between ASGI and WSGI and why FastAPI uses ASGI.

**22.** Describe the three main components of a Pydantic model and explain how they work together.

**23.** Write a complete FastAPI endpoint that:
- Accepts a path parameter `user_id` (integer)
- Accepts an optional query parameter `include_details` (boolean, default False)
- Returns a JSON response with the user data
- Includes proper status codes

---

## Module 2 Quiz: Database Integration

### Part A: Multiple Choice (1 point each)

**1. What does ORM stand for?**
a) Object-Relational Mapping
b) Object-Response Mapping
c) Operational-Resource Management
d) Object-Resource Model

**2. Which SQLAlchemy component represents the connection to the database?**
a) Session
b) Engine
c) Model
d) Base

**3. What is the N+1 query problem?**
a) Running N+1 queries instead of 1
b) Using too many joins in a query
c) Not having enough indexes
d) Querying the database too many times

**4. Which relationship loading strategy is recommended for most cases?**
a) `lazy="joined"`
b) `lazy="select"`
c) `lazy="selectin"`
d) `lazy="dynamic"`

**5. What is the purpose of Alembic?**
a) To create database connections
b) To manage database migrations
c) To generate SQL queries
d) To optimize database performance

**6. Which SQLAlchemy 2.0 syntax is used for typed columns?**
a) `Column`
b) `mapped_column`
c) `SQLColumn`
d) `db.Column`

**7. What is the correct way to define a foreign key in SQLAlchemy?**
a) `user_id = Column(ForeignKey("users.id"))`
b) `user_id = Column(foreign_key="users.id")`
c) `user_id = ForeignKey(Column("users.id"))`
d) `user_id = Relationship("users.id")`

**8. What is the purpose of the Repository pattern?**
a) To create database tables
b) To abstract database operations
c) To handle authentication
d) To manage API routes

**9. Which of the following is NOT a valid SQLAlchemy column type?**
a) String
b) Text
c) Email
d) DateTime

**10. What is the difference between `session.commit()` and `session.flush()`?**
a) `flush()` writes to the database but `commit()` doesn't
b) `commit()` writes to the database while `flush()` prepares the write
c) They are the same thing
d) `flush()` closes the session

### Part B: True/False (1 point each)

**11.** SQLAlchemy 2.0 does not support asynchronous operations.
a) True
b) False

**12.** A many-to-many relationship in SQLAlchemy always requires an association table.
a) True
b) False

**13.** The `lazy="selectin"` strategy loads related data in a separate query using an IN clause.
a) True
b) False

**14.** Alembic migrations must be manually written and cannot be auto-generated.
a) True
b) False

**15.** The Service layer typically sits between the Repository and the API endpoint.
a) True
b) False

### Part C: Fill in the Blank (2 points each)

**16.** SQLAlchemy's async engine is created using _______________.

**17.** The SQLAlchemy base class is created using _______________.

**18.** In the Repository pattern, the _______________ layer contains business logic.

**19.** Alembic migration files are stored in the _______________ directory.

**20.** The SQLAlchemy relationship parameter that defines what happens when a parent is deleted is _______________.

### Part D: Short Answer (5 points each)

**21.** Explain the difference between one-to-many and many-to-many relationships in SQLAlchemy, providing code examples for both.

**22.** Describe the three layers of the Repository pattern (Repository, Service, API) and explain why this separation is beneficial.

**23.** Write a complete SQLAlchemy model for a `Project` that:
- Has an auto-incrementing primary key
- Has a name (string, max 100, required)
- Has a description (text, optional)
- Has a one-to-many relationship with `Task`
- Includes proper constraints and indexes

---

## Module 3 Quiz: Authentication & Security

### Part A: Multiple Choice (1 point each)

**1. What does JWT stand for?**
a) JSON Web Token
b) JavaScript Web Token
c) Java Web Token
d) JSON With Token

**2. Which component of a JWT contains the actual data?**
a) Header
b) Payload
c) Signature
d) All of the above

**3. What is the purpose of refresh tokens?**
a) To improve security
b) To get new access tokens without re-authenticating
c) To refresh the user interface
d) To clear cookies

**4. Which of the following is NOT a valid password hashing algorithm?**
a) bcrypt
b) MD5
c) Argon2
d) PBKDF2

**5. What is the correct way to hash a password with passlib?**
a) `pwd_context.hash(password)`
b) `pwd_context.encrypt(password)`
c) `pwd_context.encode(password)`
d) `pwd_context.create(password)`

**6. Which decorator is used to protect an endpoint with role-based access?**
a) `@require_roles`
b) `@role_required`
c) `@dependencies`
d) No decorator - use `dependencies` parameter

**7. What is the purpose of the `oauth2_scheme` in FastAPI?**
a) To create OAuth2 tokens
b) To validate OAuth2 tokens
c) To define the token URL
d) To handle OAuth2 login

**8. Which HTTP header is used to send a JWT token?**
a) `X-Token`
b) `Authorization`
c) `Authentication`
d) `Bearer`

**9. What is the typical expiration time for an access token?**
a) 5 minutes
b) 15-30 minutes
c) 24 hours
d) 7 days

**10. What is the purpose of role-based access control (RBAC)?**
a) To authenticate users
b) To authorize users based on their role
c) To encrypt passwords
d) To manage user sessions

### Part B: True/False (1 point each)

**11.** JWT tokens should be stored in localStorage for maximum security.
a) True
b) False

**12.** Refresh tokens typically have a longer expiration time than access tokens.
a) True
b) False

**13.** The `passlib` library is used for JWT token generation.
a) True
b) False

**14.** A superuser should be able to bypass all role checks.
a) True
b) False

**15.** OAuth2 password flow is the only authentication method supported by FastAPI.
a) True
b) False

### Part C: Fill in the Blank (2 points each)

**16.** The JWT token is verified using the _______________.

**17.** Password hashing with bcrypt automatically includes a _______________.

**18.** In FastAPI, the OAuth2PasswordBearer class requires the _______________ parameter.

**19.** The three parts of a JWT token are separated by _______________.

**20.** The Python library commonly used for JWTs is _______________.

### Part D: Short Answer (5 points each)

**21.** Explain the complete OAuth2 password flow with JWT, including how tokens are created, validated, and refreshed.

**22.** Write the code for a `get_current_user` dependency that extracts a user from a JWT token, including proper error handling.

**23.** Design a role-based access control system with the following requirements:
- Users have roles: Admin, Manager, Developer, Viewer
- Admin can do everything
- Manager can create, read, update tasks
- Developer can create, read, update their own tasks
- Viewer can only read tasks

---

## Module 4 Quiz: Advanced Features

### Part A: Multiple Choice (1 point each)

**1. What is the purpose of `asyncio.gather()`?**
a) To run coroutines sequentially
b) To run coroutines concurrently
c) To create new coroutines
d) To cancel running coroutines

**2. Which of the following is a CPU-bound operation?**
a) Database query
b) File I/O
c) Mathematical computation
d) Network request

**3. What is the correct way to handle a timeout in async code?**
a) `asyncio.timeout()`
b) `asyncio.wait_for()`
c) `asyncio.sleep()`
d) `asyncio.timeout_at()`

**4. When should you use Celery instead of FastAPI's BackgroundTasks?**
a) For simple email sending
b) For distributed and persistent tasks
c) For WebSocket messages
d) For error handling

**5. What is the main advantage of WebSockets?**
a) They are faster than HTTP
b) They allow bidirectional communication
c) They work without authentication
d) They don't need a server

**6. Which Redis command sets a key with expiration?**
a) `SET`
b) `SETEX`
c) `GETSET`
d) `SETNX`

**7. What is the purpose of a semaphore in async code?**
a) To limit concurrency
b) To speed up execution
c) To handle errors
d) To create tasks

**8. Which of the following is NOT a valid cache invalidation strategy?**
a) Time-based expiration (TTL)
b) Event-based invalidation
c) Manual invalidation
d) Never invalidate

**9. What is the purpose of `asyncio.create_task()`?**
a) To create a new coroutine
b) To schedule a coroutine for execution
c) To cancel a running coroutine
d) To check if a coroutine is running

**10. Which WebSocket message type indicates a client is closing the connection?**
a) `close`
b) `disconnect`
c) `terminate`
d) `exit`

### Part B: True/False (1 point each)

**11.** Async/await can speed up CPU-bound operations.
a) True
b) False

**12.** WebSockets use the same HTTP protocol as REST APIs.
a) True
b) False

**13.** Redis is an in-memory database commonly used for caching.
a) True
b) False

**14.** Celery tasks can be scheduled to run at specific times.
a) True
b) False

**15.** The `BackgroundTasks` class in FastAPI is suitable for long-running distributed tasks.
a) True
b) False

### Part C: Fill in the Blank (2 points each)

**16.** The Redis command to delete a key is _______________.

**17.** In Celery, the _______________ is used to store task results.

**18.** The WebSocket connection manager's _______________ method sends messages to all connected clients.

**19.** The async pattern for limiting concurrent operations uses _______________.

**20.** Celery tasks are defined using the _______________ decorator.

### Part D: Short Answer (5 points each)

**21.** Explain the difference between `asyncio.gather()` and `asyncio.create_task()`. When would you use each?

**22.** Design a complete Redis caching system for a user profile endpoint with the following requirements:
- Cache for 5 minutes
- Invalidated when user updates their profile
- Uses a cache decorator pattern

**23.** Write a complete WebSocket implementation that:
- Accepts WebSocket connections from authenticated users
- Handles room joining and leaving
- Broadcasts messages to all users in a room

---

## Module 5 Quiz: Testing & Deployment

### Part A: Multiple Choice (1 point each)

**1. What is the testing pyramid?**
a) A testing framework
b) A distribution of test types
c) A debugging tool
d) A deployment strategy

**2. Which type of test is fastest to run?**
a) Unit tests
b) Integration tests
c) End-to-end tests
d) All are the same speed

**3. What is the purpose of `pytest.fixture`?**
a) To create test functions
b) To provide reusable test setup
c) To mock external services
d) To generate test reports

**4. Which pytest marker is used for async tests?**
a) `@pytest.async`
b) `@pytest.mark.async`
c) `@pytest.mark.asyncio`
d) `@pytest.asyncio`

**5. What is the purpose of multi-stage builds in Docker?**
a) Faster builds
b) Smaller images
c) Both a and b
d) Neither a nor b

**6. Which Docker Compose command rebuilds and starts containers?**
a) `docker-compose up`
b) `docker-compose up --build`
c) `docker-compose build`
d) `docker-compose start`

**7. What is the purpose of a liveness probe in Kubernetes?**
a) To check if the application is ready to accept traffic
b) To check if the application is still running
c) To check if the database is connected
d) To check if the application has the latest code

**8. Which GitHub Actions step runs after a push to the main branch?**
a) `on: push`
b) `on: pull_request`
c) `on: main`
d) `on: deploy`

**9. What is the purpose of code coverage in testing?**
a) To measure how much code is tested
b) To improve code quality
c) To find bugs
d) To generate documentation

**10. Which tool is used for end-to-end testing in FastAPI?**
a) pytest
b) httpx
c) TestClient
d) All of the above

### Part B: True/False (1 point each)

**11.** Integration tests typically run faster than unit tests.
a) True
b) False

**12.** Docker Compose can manage multiple containers as a single application.
a) True
b) False

**13.** A Kubernetes Deployment automatically manages rolling updates.
a) True
b) False

**14.** The `@pytest.mark.asyncio` decorator is required for testing async functions.
a) True
b) False

**15.** GitHub Actions workflows run only when a pull request is opened.
a) True
b) False

### Part C: Fill in the Blank (2 points each)

**16.** The Docker command to view running containers is _______________.

**17.** In Kubernetes, a _______________ exposes pods to the network.

**18.** The GitHub Actions workflow file is stored in the _______________ directory.

**19.** The pytest command to run tests with coverage is _______________.

**20.** A Kubernetes _______________ manages the number of replicas running.

### Part D: Short Answer (5 points each)

**21.** Explain the difference between a liveness probe and a readiness probe in Kubernetes. When would you use each?

**22.** Write a complete Dockerfile for a FastAPI application that:
- Uses multi-stage builds
- Installs dependencies in a builder stage
- Runs the application with gunicorn
- Is optimized for production

**23.** Design a GitHub Actions CI/CD pipeline that:
- Runs on push to main and pull requests
- Installs dependencies
- Runs linting and tests
- Builds a Docker image
- Deploys to Kubernetes on main branch

---

## Mid-Term Exam (Module 1-3)

### Part A: Multiple Choice (1 point each)

**1. Which of the following is NOT a feature of FastAPI?**
a) Automatic OpenAPI documentation
b) Built-in GraphQL support
c) Data validation with Pydantic
d) Async/await support

**2. What is the correct way to create an optional query parameter in FastAPI?**
a) `q: str`
b) `q: str = None`
c) `q: str = "default"`
d) `q: str = Query(...)`

**3. Which status code should be returned when a resource is created?**
a) 200 OK
b) 201 Created
c) 204 No Content
d) 302 Found

**4. What is the purpose of the `response_model` parameter in FastAPI?**
a) To validate the request body
b) To control the response format
c) To specify the database model
d) To handle errors

**5. Which SQLAlchemy component represents a transaction?**
a) Engine
b) Session
c) Model
d) Base

**6. What is the correct SQLAlchemy relationship for a one-to-many where a User has many Tasks?**
a) `tasks = relationship("Task", back_populates="user")`
b) `tasks = relationship("Task", uselist=True)`
c) `tasks = Column(ForeignKey("users.id"))`
d) `tasks = many_to_one("Task")`

**7. Which of the following is a valid way to hash a password in FastAPI?**
a) `hashlib.md5(password.encode())`
b) `pwd_context.hash(password)`
c) `jwt.encode(password)`
d) `bcrypt(password)`

**8. What is the purpose of refresh tokens?**
a) To get new access tokens without re-authenticating
b) To refresh the user interface
c) To improve performance
d) To handle errors

**9. Which component of a JWT contains the expiration time?**
a) Header
b) Payload
c) Signature
d) None of the above

**10. What is the correct way to protect an endpoint with role-based access?**
a) `@role_required("admin")`
b) `dependencies=[Depends(PermissionChecker(["admin"]))]`
c) `@requires_role("admin")`
d) `@check_role("admin")`

### Part B: True/False (1 point each)

**11.** FastAPI automatically validates request bodies using Pydantic models.
a) True
b) False

**12.** SQLAlchemy 2.0 does not support asynchronous operations.
a) True
b) False

**13.** The `lazy="selectin"` strategy helps solve the N+1 query problem.
a) True
b) False

**14.** JWT tokens should be stored in localStorage for security.
a) True
b) False

**15.** The Repository pattern separates business logic from database operations.
a) True
b) False

### Part C: Short Answer (5 points each)

**16.** Explain the complete request-response lifecycle in FastAPI, from the incoming HTTP request to the outgoing response.

**17.** Design a complete user registration system with the following requirements:
- Pydantic model with validation
- Password hashing
- Database storage
- Email confirmation (simulated)
- Error handling

**18.** Compare and contrast the Repository pattern and the Service layer. Why are they both necessary in a well-designed application?

**19.** Explain OAuth2 password flow with JWT tokens, including:
- The login process
- Token creation
- Token validation
- Token refresh
- Security considerations

**20.** Write the code for a complete CRUD API for a `Task` entity including:
- Pydantic schemas for create, update, and response
- SQLAlchemy model
- Repository methods
- Service layer
- FastAPI endpoints
- Proper error handling

---

## Final Exam: Comprehensive

### Part A: Multiple Choice (1 point each)

**1. What is the main purpose of Pydantic in FastAPI?**
a) Database connectivity
b) Data validation and serialization
c) Authentication
d) WebSocket handling

**2. Which HTTP method is idempotent?**
a) POST
b) PUT
c) PATCH
d) All of the above

**3. What is the correct status code for "Bad Request"?**
a) 400
b) 401
c) 403
d) 404

**4. Which SQLAlchemy strategy is recommended for loading relationships?**
a) `lazy="joined"`
b) `lazy="select"`
c) `lazy="selectin"`
d) `lazy="dynamic"`

**5. What is the purpose of Alembic?**
a) Database migrations
b) Database connections
c) Database queries
d) Database optimization

**6. Which of the following is NOT a valid Pydantic field constraint?**
a) `min_length`
b) `gt`
c) `max`
d) `pattern`

**7. What is the correct way to handle exceptions in async code?**
a) `try/except` around `await`
b) `catch` statement
c) `on_error` callback
d) `error` handler

**8. Which tool is used for distributed task queues?**
a) Celery
b) Redis
c) RabbitMQ
d) All of the above

**9. What is the purpose of a connection manager in WebSockets?**
a) To handle HTTP connections
b) To manage active WebSocket connections
c) To connect to the database
d) To manage authentication

**10. Which Redis command is used for caching with expiration?**
a) `SET`
b) `SETEX`
c) `GETSET`
d) `SETNX`

**11. What is the testing pyramid?**
a) A testing framework
b) A distribution of test types
c) A debugging tool
d) A deployment strategy

**12. Which Docker command builds an image from a Dockerfile?**
a) `docker build`
b) `docker create`
c) `docker make`
d) `docker image`

**13. What is the purpose of a Kubernetes Service?**
a) To expose pods to the network
b) To manage pod replicas
c) To store configuration
d) To handle storage

**14. Which of the following is NOT a benefit of Clean Architecture?**
a) Framework independence
b) Testability
c) Faster execution speed
d) Maintainability

**15. What is the purpose of domain events?**
a) To store data
b) To communicate changes between components
c) To handle errors
d) To authenticate users

### Part B: True/False (1 point each)

**16.** FastAPI uses Starlette under the hood.
a) True
b) False

**17.** Pydantic V2 is slower than Pydantic V1.
a) True
b) False

**18.** Async/await can speed up CPU-bound operations.
a) True
b) False

**19.** WebSockets use the same protocol as HTTP.
a) True
b) False

**20.** Celery requires a message broker like Redis or RabbitMQ.
a) True
b) False

**21.** Docker Compose can manage multiple containers as a single application.
a) True
b) False

**22.** Kubernetes Deployments automatically handle rolling updates.
a) True
b) False

**23.** The Repository pattern is part of Clean Architecture.
a) True
b) False

**24.** Domain-Driven Design (DDD) is only useful for large applications.
a) True
b) False

**25.** Event-driven architecture improves system decoupling.
a) True
b) False

### Part C: Short Answer (5 points each)

**26.** Explain the difference between WSGI and ASGI, and why FastAPI uses ASGI.

**27.** Describe the three layers of the Repository pattern (Repository, Service, API) and explain how they work together.

**28.** Explain the complete OAuth2 password flow with JWT tokens, including token creation, validation, and refresh.

**29.** Compare and contrast FastAPI BackgroundTasks and Celery. When would you use each?

**30.** Explain the difference between WebSockets and HTTP requests. Provide a use case for each.

**31.** Design a complete caching strategy for a high-traffic API that includes:
- Cache tiers (memory, Redis)
- Cache invalidation
- Cache warming
- Performance considerations

**32.** Explain the testing pyramid and describe the different types of tests in a FastAPI application.

**33.** Describe the components of Docker and Kubernetes and how they work together for application deployment.

**34.** Explain the principles of Clean Architecture and how they apply to FastAPI applications.

**35.** Describe event-driven architecture and its benefits for microservices.

### Part D: Practical Coding Challenges (10 points each)

**36. User Authentication System**

Write a complete authentication system with:
- User registration with password hashing
- Login endpoint returning JWT
- Token validation dependency
- Role-based access control
- Refresh token functionality

```python
# Your answer here
```

**37. Task Management API**

Write a complete task management API with:
- SQLAlchemy model for Task
- Repository pattern implementation
- Service layer
- FastAPI endpoints (CRUD)
- Proper error handling
- Filtering and pagination

```python
# Your answer here
```

**38. Caching Implementation**

Write a Redis caching system with:
- Cache decorator
- TTL management
- Cache invalidation
- Integration with a FastAPI endpoint

```python
# Your answer here
```

**39. WebSocket Chat**

Write a WebSocket chat system with:
- Connection manager
- Room management
- Message broadcasting
- User authentication

```python
# Your answer here
```

**40. Complete CI/CD Pipeline**

Write a GitHub Actions workflow for:
- Linting and formatting
- Running tests
- Building Docker image
- Deploying to Kubernetes

```yaml
# Your answer here
```

---

## Answer Keys

### Module 1 Answer Key

**Part A: Multiple Choice**
1. a) Asynchronous Server Gateway Interface
2. b) Built-in support for GraphQL
3. c) GET
4. c) `name: str = Field(...)`
5. b) 201 Created
6. a) `@app.get("/items/{item_id}")`
7. b) To improve testability and reusability
8. b) `@app.post`
9. c) `raise HTTPException(status_code=404, detail="Not found")`
10. b) `min_length=3`

**Part B: True/False**
11. a) True
12. b) False (ASGI is newer and supports async)
13. b) False (Query parameters are optional unless specified)
14. b) False (`@field_validator` validates single fields)
15. a) True

**Part C: Fill in the Blank**
16. Swagger UI (or OpenAPI)
17. default value
18. `gt` (greater than)
19. 404
20. `fastapi`

**Part D: Short Answer**

**21.** ASGI (Asynchronous Server Gateway Interface) is the modern successor to WSGI (Web Server Gateway Interface). WSGI is synchronous and handles one request at a time, while ASGI supports asynchronous programming, allowing multiple requests to be handled concurrently. FastAPI uses ASGI because it enables high performance, WebSocket support, and efficient handling of I/O-bound operations.

**22.** The three main components of a Pydantic model are:
1. **Fields**: Defined with type hints and optional `Field()` constraints
2. **Validators**: `@field_validator` for single fields, `@model_validator` for cross-field validation
3. **Configuration**: `model_config` for global settings

These work together to automatically validate, convert, and serialize data.

**23.**
```python
from fastapi import FastAPI, HTTPException, status
from pydantic import BaseModel
from typing import Optional

app = FastAPI()

class UserResponse(BaseModel):
    id: int
    username: str
    email: str
    include_details: Optional[bool] = None

@app.get("/users/{user_id}", response_model=UserResponse)
async def get_user(user_id: int, include_details: bool = False):
    # Simulate user lookup
    user = {"id": user_id, "username": f"user_{user_id}", "email": f"user_{user_id}@example.com"}
    if not user:
        raise HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail="User not found")
    return user
```

---

### Module 2 Answer Key

**Part A: Multiple Choice**
1. a) Object-Relational Mapping
2. b) Engine
3. a) Running N+1 queries instead of 1
4. c) `lazy="selectin"`
5. b) To manage database migrations
6. b) `mapped_column`
7. a) `user_id = Column(ForeignKey("users.id"))`
8. b) To abstract database operations
9. c) Email
10. b) `commit()` writes to the database while `flush()` prepares the write

**Part B: True/False**
11. b) False (SQLAlchemy 2.0 fully supports async)
12. a) True
13. a) True
14. b) False (Alembic can auto-generate migrations)
15. a) True

**Part C: Fill in the Blank**
16. `create_async_engine()`
17. `declarative_base()`
18. Service
19. `alembic/versions`
20. `ondelete`

**Part D: Short Answer**

**21.** One-to-many relationships are used when one record in a table can be related to many records in another table. Example: A User has many Tasks. Many-to-many relationships are used when records in both tables can be related to multiple records in the other table. Example: Users can have many Roles and Roles can have many Users.

One-to-many code:
```python
class User(Base):
    tasks = relationship("Task", back_populates="user")

class Task(Base):
    user_id = Column(ForeignKey("users.id"))
    user = relationship("User", back_populates="tasks")
```

Many-to-many code:
```python
association = Table("user_roles", Base.metadata,
    Column("user_id", ForeignKey("users.id")),
    Column("role_id", ForeignKey("roles.id"))
)

class User(Base):
    roles = relationship("Role", secondary=association)
```

**22.** The three layers are:
1. **Repository Layer**: Handles database operations, provides CRUD methods
2. **Service Layer**: Contains business logic, orchestrates repositories
3. **API Layer**: FastAPI endpoints, handles HTTP requests/responses

This separation is beneficial because it:
- Makes code more maintainable
- Improves testability (each layer can be tested independently)
- Separates concerns (business logic is isolated from HTTP and database code)
- Makes it easier to change the database or API without affecting business logic

**23.**
```python
from sqlalchemy import Column, Integer, String, Text, ForeignKey, Index
from sqlalchemy.orm import relationship
from app.core.database import Base

class Project(Base):
    __tablename__ = "projects"
    
    id = Column(Integer, primary_key=True, index=True)
    name = Column(String(100), nullable=False)
    description = Column(Text, nullable=True)
    
    # Relationship
    tasks = relationship("Task", back_populates="project")
    
    # Indexes
    __table_args__ = (
        Index("idx_projects_name", "name"),
    )
```

---

### Module 3 Answer Key

**Part A: Multiple Choice**
1. a) JSON Web Token
2. b) Payload
3. b) To get new access tokens without re-authenticating
4. b) MD5
5. a) `pwd_context.hash(password)`
6. d) No decorator - use `dependencies` parameter
7. c) To define the token URL
8. b) `Authorization`
9. b) 15-30 minutes
10. b) To authorize users based on their role

**Part B: True/False**
11. b) False (Should use HttpOnly cookies)
12. a) True
13. b) False (passlib is for password hashing, python-jose is for JWT)
14. a) True
15. b) False (FastAPI supports multiple authentication methods)

**Part C: Fill in the Blank**
16. secret key
17. salt
18. `tokenUrl`
19. periods (.)
20. `python-jose`

**Part D: Short Answer**

**21.** The OAuth2 password flow with JWT works as follows:

1. **Login**: Client sends username/password to `/login` endpoint
2. **Validation**: Server validates credentials
3. **Token Creation**: Server creates a JWT access token (short-lived) and a refresh token (long-lived)
4. **Token Response**: Server returns both tokens to client
5. **API Access**: Client includes access token in `Authorization: Bearer <token>` header
6. **Token Validation**: Server validates token on each request using `get_current_user` dependency
7. **Token Refresh**: When access token expires, client sends refresh token to `/refresh` endpoint for a new access token

**22.**
```python
async def get_current_user(token: str = Depends(oauth2_scheme)):
    try:
        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
        user_id = payload.get("sub")
        if user_id is None:
            raise HTTPException(status_code=401, detail="Invalid token")
    except JWTError:
        raise HTTPException(status_code=401, detail="Invalid token")
    
    user = await get_user_by_id(user_id)
    if user is None:
        raise HTTPException(status_code=401, detail="User not found")
    
    if not user.is_active:
        raise HTTPException(status_code=401, detail="Inactive user")
    
    return user
```

**23.**
```python
class UserRole(str, Enum):
    ADMIN = "admin"
    MANAGER = "manager"
    DEVELOPER = "developer"
    VIEWER = "viewer"

class PermissionChecker:
    def __init__(self, allowed_roles: list, allow_superuser: bool = True):
        self.allowed_roles = allowed_roles
        self.allow_superuser = allow_superuser
    
    async def __call__(self, current_user: User = Depends(get_current_user)):
        if self.allow_superuser and current_user.is_superuser:
            return current_user
        
        if current_user.role not in self.allowed_roles:
            raise HTTPException(status_code=403, detail="Insufficient permissions")
        return current_user

# Usage
require_admin = PermissionChecker([UserRole.ADMIN])
require_manager = PermissionChecker([UserRole.ADMIN, UserRole.MANAGER])
require_developer = PermissionChecker([UserRole.ADMIN, UserRole.MANAGER, UserRole.DEVELOPER])
require_viewer = PermissionChecker([UserRole.ADMIN, UserRole.MANAGER, UserRole.DEVELOPER, UserRole.VIEWER])

@router.post("/tasks/", dependencies=[Depends(require_manager)])
async def create_task(...):
    pass

@router.get("/tasks/", dependencies=[Depends(require_viewer)])
async def list_tasks(...):
    pass
```

---

### Module 4 Answer Key

**Part A: Multiple Choice**
1. b) To run coroutines concurrently
2. c) Mathematical computation
3. b) `asyncio.wait_for()`
4. b) For distributed and persistent tasks
5. b) They allow bidirectional communication
6. b) `SETEX`
7. a) To limit concurrency
8. d) Never invalidate
9. b) To schedule a coroutine for execution
10. a) `close`

**Part B: True/False**
11. b) False (Async is for I/O-bound, not CPU-bound)
12. b) False (WebSockets use the WebSocket protocol)
13. a) True
14. a) True
15. b) False (BackgroundTasks is for simple, short operations)

**Part C: Fill in the Blank**
16. `DEL`
17. backend
18. `broadcast`
19. `asyncio.Semaphore`
20. `@celery_app.task`

**Part D: Short Answer**

**21.** `asyncio.gather()` runs multiple coroutines concurrently and waits for all to complete before returning. It's used when you have a list of coroutines to run in parallel. `asyncio.create_task()` schedules a single coroutine to run in the background and returns a Task object that can be awaited later.

Use `gather()` when you need to run multiple coroutines and wait for all results. Use `create_task()` when you want to start a background operation but don't need to wait for it immediately.

**22.**
```python
import redis.asyncio as redis
import json
from functools import wraps

class RedisCache:
    def __init__(self, redis_url: str):
        self.client = redis.from_url(redis_url)
    
    async def get(self, key: str):
        return await self.client.get(key)
    
    async def set(self, key: str, value: str, ttl: int = 300):
        await self.client.setex(key, ttl, value)
    
    async def delete(self, key: str):
        await self.client.delete(key)

def cached(ttl: int = 300, prefix: str = ""):
    def decorator(func):
        @wraps(func)
        async def wrapper(*args, **kwargs):
            cache = RedisCache("redis://localhost:6379/0")
            key = f"{prefix}:{func.__name__}:{hash(str(args) + str(kwargs))}"
            
            cached_value = await cache.get(key)
            if cached_value:
                return json.loads(cached_value)
            
            result = await func(*args, **kwargs)
            await cache.set(key, json.dumps(result), ttl)
            return result
        return wrapper
    return decorator

# Invalidation
async def invalidate_user_cache(user_id: int):
    cache = RedisCache("redis://localhost:6379/0")
    await cache.delete(f"user:get_user_profile:{user_id}")
    await cache.delete(f"user:get_user_tasks:{user_id}")
```

**23.**
```python
from fastapi import WebSocket, WebSocketDisconnect
from typing import Dict, Set

class ConnectionManager:
    def __init__(self):
        self.active_connections: Dict[str, Set[WebSocket]] = {}
    
    async def connect(self, websocket: WebSocket, room: str):
        await websocket.accept()
        if room not in self.active_connections:
            self.active_connections[room] = set()
        self.active_connections[room].add(websocket)
    
    def disconnect(self, websocket: WebSocket, room: str):
        if room in self.active_connections:
            self.active_connections[room].discard(websocket)
    
    async def broadcast_to_room(self, message: str, room: str):
        if room in self.active_connections:
            for websocket in self.active_connections[room]:
                await websocket.send_text(message)

manager = ConnectionManager()

@router.websocket("/ws/{room}")
async def websocket_endpoint(websocket: WebSocket, room: str):
    await manager.connect(websocket, room)
    try:
        while True:
            data = await websocket.receive_text()
            await manager.broadcast_to_room(data, room)
    except WebSocketDisconnect:
        manager.disconnect(websocket, room)
```

---

### Module 5 Answer Key

**Part A: Multiple Choice**
1. b) A distribution of test types
2. a) Unit tests
3. b) To provide reusable test setup
4. c) `@pytest.mark.asyncio`
5. c) Both a and b
6. b) `docker-compose up --build`
7. b) To check if the application is still running
8. a) `on: push`
9. a) To measure how much code is tested
10. d) All of the above

**Part B: True/False**
11. b) False (Unit tests are faster)
12. a) True
13. a) True
14. a) True
15. b) False (Workflows can be triggered by multiple events)

**Part C: Fill in the Blank**
16. `docker ps`
17. Service
18. `.github/workflows`
19. `pytest --cov=app`
20. Deployment

**Part D: Short Answer**

**21.** A liveness probe checks if the application is still running and should be restarted if it fails. A readiness probe checks if the application is ready to accept traffic and should be removed from the load balancer if it fails.

Use liveness probes to detect and restart hung or crashed applications. Use readiness probes for applications that need time to initialize or might become temporarily unavailable (e.g., during database migrations or heavy processing).

**22.**
```dockerfile
# Builder stage
FROM python:3.11-slim as builder

WORKDIR /app
COPY requirements.txt .
RUN pip install --user -r requirements.txt

# Production stage
FROM python:3.11-slim

WORKDIR /app
COPY --from=builder /root/.local /root/.local
COPY . .

ENV PATH=/root/.local/bin:$PATH
EXPOSE 8000

CMD ["gunicorn", "app.main:app", "--worker-class", "uvicorn.workers.UvicornWorker", "--workers", "4", "--bind", "0.0.0.0:8000"]
```

**23.**
```yaml
name: CI/CD Pipeline

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run linting
        run: flake8 app
      - name: Run tests
        run: pytest --cov=app
  
  build:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v3
      - name: Build Docker image
        run: docker build -t myapp .
      - name: Push to registry
        run: |
          docker tag myapp ghcr.io/username/myapp
          docker push ghcr.io/username/myapp
  
  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Deploy to Kubernetes
        run: |
          kubectl set image deployment/myapp myapp=ghcr.io/username/myapp:latest
          kubectl rollout status deployment/myapp
```

---

### Mid-Term Answer Key

**Part A: Multiple Choice**
1. b) Built-in GraphQL support
2. b) `q: str = None`
3. b) 201 Created
4. b) To control the response format
5. b) Session
6. a) `tasks = relationship("Task", back_populates="user")`
7. b) `pwd_context.hash(password)`
8. a) To get new access tokens without re-authenticating
9. b) Payload
10. b) `dependencies=[Depends(PermissionChecker(["admin"]))]`

**Part B: True/False**
11. a) True
12. b) False
13. a) True
14. b) False
15. a) True

**Part C: Short Answer**

**16.** The request-response lifecycle in FastAPI:

1. HTTP request arrives
2. Middleware processes the request (CORS, logging, etc.)
3. Router matches the request to an endpoint
4. Dependencies are resolved (database session, authentication)
5. Request data is validated (query params, path params, body)
6. Endpoint function is called
7. Business logic executes
8. Response is serialized
9. Response model validation (if specified)
10. Middleware processes the response
11. HTTP response is sent to client

**17.**
```python
from pydantic import BaseModel, EmailStr, Field, field_validator
from passlib.context import CryptContext

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")

class UserCreate(BaseModel):
    email: EmailStr
    password: str = Field(..., min_length=8)
    confirm_password: str
    
    @field_validator('password')
    @classmethod
    def validate_password(cls, v):
        if not any(c.isupper() for c in v):
            raise ValueError('Password must contain uppercase')
        return v
    
    @model_validator(mode='after')
    def validate_confirm_password(self):
        if self.password != self.confirm_password:
            raise ValueError('Passwords do not match')
        return self

class UserResponse(BaseModel):
    id: int
    email: str
    created_at: datetime

class UserService:
    def __init__(self, session: AsyncSession):
        self.repo = UserRepository(session)
        self.session = session
    
    async def register_user(self, data: UserCreate) -> User:
        if await self.repo.get_by_email(data.email):
            raise HTTPException(400, "Email already exists")
        
        user = await self.repo.create(
            email=data.email,
            hashed_password=pwd_context.hash(data.password)
        )
        await self.session.commit()
        
        # Send confirmation email (background)
        return user

@app.post("/register", response_model=UserResponse)
async def register(data: UserCreate, service: UserService = Depends(get_user_service)):
    user = await service.register_user(data)
    return user
```

**18.** The Repository pattern abstracts database operations, providing methods like `create()`, `get()`, `update()`, and `delete()`. The Service layer contains business logic and orchestrates repositories.

Both are necessary because:
- **Repository**: Handles database-specific operations, making it easy to change databases
- **Service**: Contains business logic independent of the database, making it reusable and testable
- Separation ensures each layer has a single responsibility
- Makes testing easier (mock repository for service tests)

**19.** OAuth2 password flow with JWT:

1. **Login**: POST `/login` with username/password
2. **Validation**: Check credentials against database
3. **Token Creation**: Create JWT with `sub` (user_id), `exp` (expiration)
4. **Response**: Return access_token and refresh_token
5. **API Access**: Client sends `Authorization: Bearer <token>`
6. **Validation**: Decode JWT, check signature, verify expiration
7. **Token Refresh**: Use refresh_token to get new access_token

Security considerations:
- Tokens should be short-lived (15-30 min)
- Use HTTPS to prevent interception
- Store tokens securely (HttpOnly cookies)
- Implement token revocation
- Use strong secret keys

**20.**
```python
# Schemas
class TaskCreate(BaseModel):
    title: str = Field(..., min_length=1)
    description: str = None

class TaskUpdate(BaseModel):
    title: str = Field(None, min_length=1)
    description: str = None
    status: str = None

class TaskResponse(BaseModel):
    id: int
    title: str
    description: str
    status: str
    created_at: datetime
    updated_at: datetime

# Model
class Task(Base):
    __tablename__ = "tasks"
    id = Column(Integer, primary_key=True)
    title = Column(String(200), nullable=False)
    description = Column(Text)
    status = Column(String(20), default="todo")
    created_at = Column(DateTime, server_default=func.now())
    updated_at = Column(DateTime, onupdate=func.now())

# Repository
class TaskRepository:
    def __init__(self, session: AsyncSession):
        self.session = session
    
    async def create(self, **kwargs) -> Task:
        task = Task(**kwargs)
        self.session.add(task)
        await self.session.flush()
        return task
    
    async def get(self, id: int) -> Optional[Task]:
        result = await self.session.execute(select(Task).where(Task.id == id))
        return result.scalar_one_or_none()
    
    async def get_all(self, skip: int = 0, limit: int = 100) -> list[Task]:
        result = await self.session.execute(select(Task).offset(skip).limit(limit))
        return result.scalars().all()

# Service
class TaskService:
    def __init__(self, session: AsyncSession):
        self.repo = TaskRepository(session)
        self.session = session
    
    async def create_task(self, data: TaskCreate) -> Task:
        return await self.repo.create(**data.model_dump())

# API
@router.post("/tasks", response_model=TaskResponse, status_code=201)
async def create_task(data: TaskCreate, service: TaskService = Depends(get_task_service)):
    task = await service.create_task(data)
    await service.session.commit()
    return task

@router.get("/tasks", response_model=list[TaskResponse])
async def list_tasks(skip: int = 0, limit: int = 100, service: TaskService = Depends(get_task_service)):
    tasks = await service.repo.get_all(skip=skip, limit=limit)
    return tasks

@router.get("/tasks/{task_id}", response_model=TaskResponse)
async def get_task(task_id: int, service: TaskService = Depends(get_task_service)):
    task = await service.repo.get(task_id)
    if not task:
        raise HTTPException(404, "Task not found")
    return task
```

---

### Final Exam Answer Key

**Part A: Multiple Choice**
1. b) Data validation and serialization
2. b) PUT
3. a) 400
4. c) `lazy="selectin"`
5. a) Database migrations
6. c) `max`
7. a) `try/except` around `await`
8. d) All of the above
9. b) To manage active WebSocket connections
10. b) `SETEX`
11. b) A distribution of test types
12. a) `docker build`
13. a) To expose pods to the network
14. c) Faster execution speed
15. b) To communicate changes between components

**Part B: True/False**
16. a) True
17. b) False
18. b) False
19. b) False
20. a) True
21. a) True
22. a) True
23. a) True
24. b) False
25. a) True

**Part C: Short Answer**

**26.** WSGI (Web Server Gateway Interface) is synchronous and handles one request at a time. ASGI (Asynchronous Server Gateway Interface) supports asynchronous programming, allowing multiple requests to be handled concurrently. FastAPI uses ASGI because it:
- Enables high performance with async/await
- Supports WebSockets
- Handles I/O-bound operations efficiently
- Provides better scalability

**27.** See Module 2, Question 22.

**28.** See Module 3, Question 21.

**29.** BackgroundTasks is built into FastAPI for simple, short operations (sending emails, processing small files). Celery is a distributed task queue for long-running, persistent tasks.

Use BackgroundTasks when:
- Tasks are simple and quick
- You don't need task persistence
- You don't need monitoring

Use Celery when:
- Tasks are complex or long-running
- You need task persistence
- You need monitoring and retry mechanisms
- You need to distribute tasks across multiple workers

**30.** HTTP requests are stateless, request-response based, and require the client to initiate communication. WebSockets provide full-duplex, stateful, bidirectional communication where both sides can send messages at any time.

Use HTTP for:
- Simple API calls
- CRUD operations
- When you don't need real-time features

Use WebSockets for:
- Real-time chat
- Live updates
- Gaming
- Financial data streaming
- Notifications

**31.**
```python
# Cache tiers: Memory (L1) + Redis (L2)
class MultiLevelCache:
    def __init__(self):
        self.memory = MemoryCache(max_size=1000, ttl=60)
        self.redis = RedisCache(redis_url)
    
    async def get(self, key):
        # Try L1
        value = await self.memory.get(key)
        if value:
            return value
        # Try L2
        value = await self.redis.get(key)
        if value:
            await self.memory.set(key, value)
        return value

# Cache decorator with invalidation
@cached(ttl=300, prefix="user")
async def get_user(user_id: int):
    return await db.fetch_user(user_id)

# Warmup
async def warmup_cache():
    popular_users = await db.get_popular_users()
    for user in popular_users:
        await get_user(user.id, _cache=True)

# Invalidation on update
async def update_user(user_id: int, data):
    await db.update_user(user_id, data)
    await invalidate_cache(f"user:get_user:{user_id}")
```

**32.** The testing pyramid represents the distribution of test types:

1. **Unit Tests** (Base): Fast, isolated, mock dependencies, test individual functions
2. **Integration Tests** (Middle): Test interactions between components, real database
3. **End-to-End Tests** (Top): Slow, test complete workflows, full system

In FastAPI:
- Unit: Test services, models, utilities
- Integration: Test API endpoints with test database
- E2E: Test complete user flows with real services

**33.** Docker is a containerization platform that packages applications and their dependencies into isolated containers. Key components: Dockerfile (build instructions), Images (packaged applications), Containers (running instances), Volumes (persistent storage).

Kubernetes is a container orchestration platform that manages containerized applications. Key components: Pods (smallest deployable unit), Deployments (manages replicas), Services (exposes pods), Ingress (routes traffic), ConfigMaps/Secrets (configuration).

They work together: Docker builds the container images, Kubernetes deploys and manages them across a cluster of machines.

**34.** Clean Architecture principles:
1. **Independence of Frameworks**: Business logic doesn't depend on external frameworks
2. **Testability**: Business logic can be tested without UI, database, or web server
3. **Independence of UI**: The UI can change without affecting business logic
4. **Independence of Database**: Database can be changed without affecting business logic
5. **Independence of External Agencies**: External services can be changed without affecting business logic

Layers: Entities (Domain) → Use Cases (Application) → Interface Adapters → Frameworks & Drivers

**35.** Event-driven architecture is a pattern where components communicate through events. When something happens, an event is published, and interested components react to it.

Benefits:
- **Loose Coupling**: Components don't know about each other
- **Scalability**: Components can be scaled independently
- **Resilience**: Failure in one component doesn't affect others
- **Flexibility**: Easy to add new components that react to events
- **Auditability**: Events provide a natural audit trail

---

### Practical Coding Challenge Solutions

**36. User Authentication System**

```python
# app/core/security.py
from passlib.context import CryptContext
from jose import jwt, JWTError
from datetime import datetime, timedelta, timezone

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
SECRET_KEY = "your-secret-key"
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 30
REFRESH_TOKEN_EXPIRE_DAYS = 7

def verify_password(plain: str, hashed: str) -> bool:
    return pwd_context.verify(plain, hashed)

def get_password_hash(password: str) -> str:
    return pwd_context.hash(password)

def create_access_token(data: dict) -> str:
    to_encode = data.copy()
    expire = datetime.now(timezone.utc) + timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode.update({"exp": expire, "type": "access"})
    return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)

def create_refresh_token(data: dict) -> str:
    to_encode = data.copy()
    expire = datetime.now(timezone.utc) + timedelta(days=REFRESH_TOKEN_EXPIRE_DAYS)
    to_encode.update({"exp": expire, "type": "refresh"})
    return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)

def decode_token(token: str) -> dict:
    try:
        return jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
    except JWTError:
        raise HTTPException(status_code=401, detail="Invalid token")

# app/api/endpoints/auth.py
from fastapi.security import OAuth2PasswordBearer, OAuth2PasswordRequestForm

oauth2_scheme = OAuth2PasswordBearer(tokenUrl="token")

async def get_current_user(token: str = Depends(oauth2_scheme)):
    try:
        payload = decode_token(token)
        if payload.get("type") != "access":
            raise HTTPException(status_code=401)
        user_id = payload.get("sub")
        if not user_id:
            raise HTTPException(status_code=401)
    except:
        raise HTTPException(status_code=401)
    
    user = await user_service.get_user(user_id)
    if not user or not user.is_active:
        raise HTTPException(status_code=401)
    return user

@router.post("/register", status_code=201)
async def register(data: UserCreate):
    return await user_service.create_user(data)

@router.post("/token")
async def login(form_data: OAuth2PasswordRequestForm = Depends()):
    user = await user_service.authenticate(form_data.username, form_data.password)
    if not user:
        raise HTTPException(status_code=401, detail="Invalid credentials")
    
    return {
        "access_token": create_access_token({"sub": user.id}),
        "refresh_token": create_refresh_token({"sub": user.id}),
        "token_type": "bearer"
    }

@router.post("/refresh")
async def refresh_token(data: TokenRefreshRequest):
    try:
        payload = decode_token(data.refresh_token)
        if payload.get("type") != "refresh":
            raise HTTPException(status_code=401)
        user_id = payload.get("sub")
    except:
        raise HTTPException(status_code=401)
    
    return {
        "access_token": create_access_token({"sub": user_id}),
        "token_type": "bearer"
    }
```

**37. Task Management API**

```python
# Complete implementation as shown in Mid-Term Question 20, with these additions:

# app/models/task.py
class Task(Base):
    __tablename__ = "tasks"
    id = Column(Integer, primary_key=True, index=True)
    title = Column(String(200), nullable=False)
    description = Column(Text, nullable=True)
    status = Column(String(20), server_default="todo")
    priority = Column(String(20), server_default="medium")
    due_date = Column(DateTime, nullable=True)
    created_at = Column(DateTime, server_default=func.now())
    updated_at = Column(DateTime, onupdate=func.now())
    user_id = Column(Integer, ForeignKey("users.id"))

# app/crud/task.py
class TaskRepository(BaseRepository[Task]):
    def __init__(self, session: AsyncSession):
        super().__init__(Task, session)
    
    async def get_by_user(self, user_id: int, skip: int = 0, limit: int = 100):
        result = await self.session.execute(
            select(Task).where(Task.user_id == user_id).offset(skip).limit(limit)
        )
        return result.scalars().all()
    
    async def get_by_status(self, status: str, skip: int = 0, limit: int = 100):
        result = await self.session.execute(
            select(Task).where(Task.status == status).offset(skip).limit(limit)
        )
        return result.scalars().all()

# app/services/task.py
class TaskService:
    def __init__(self, session: AsyncSession):
        self.repo = TaskRepository(session)
        self.session = session
    
    async def create_task(self, data: TaskCreate, user_id: int) -> Task:
        return await self.repo.create(**data.model_dump(), user_id=user_id)
    
    async def update_task(self, task_id: int, data: TaskUpdate) -> Optional[Task]:
        task = await self.repo.get(task_id)
        if not task:
            return None
        update_data = data.model_dump(exclude_unset=True)
        for key, value in update_data.items():
            setattr(task, key, value)
        await self.session.flush()
        return task

# app/api/v1/endpoints/tasks.py
@router.post("/tasks", response_model=TaskResponse, status_code=201)
async def create_task(
    data: TaskCreate,
    current_user: User = Depends(get_current_user),
    service: TaskService = Depends(get_task_service)
):
    task = await service.create_task(data, current_user.id)
    await service.session.commit()
    return task

@router.get("/tasks", response_model=List[TaskResponse])
async def list_tasks(
    skip: int = 0,
    limit: int = 100,
    status: Optional[str] = None,
    current_user: User = Depends(get_current_user),
    service: TaskService = Depends(get_task_service)
):
    if status:
        tasks = await service.repo.get_by_status(status, skip, limit)
    else:
        tasks = await service.repo.get_by_user(current_user.id, skip, limit)
    return tasks

@router.put("/tasks/{task_id}", response_model=TaskResponse)
async def update_task(
    task_id: int,
    data: TaskUpdate,
    service: TaskService = Depends(get_task_service)
):
    task = await service.update_task(task_id, data)
    if not task:
        raise HTTPException(status_code=404, detail="Task not found")
    await service.session.commit()
    return task
```

**38. Caching Implementation**

```python
import redis.asyncio as redis
import json
from functools import wraps
from typing import Optional, Any

class RedisCache:
    def __init__(self, redis_url: str = "redis://localhost:6379/0"):
        self.client = None
        self.redis_url = redis_url
        self._connected = False
    
    async def connect(self):
        if not self._connected:
            self.client = redis.from_url(self.redis_url)
            self._connected = True
    
    async def get(self, key: str) -> Optional[Any]:
        await self.connect()
        data = await self.client.get(key)
        if data:
            return json.loads(data)
        return None
    
    async def set(self, key: str, value: Any, ttl: int = 300) -> bool:
        await self.connect()
        try:
            await self.client.setex(key, ttl, json.dumps(value))
            return True
        except Exception:
            return False
    
    async def delete(self, key: str) -> bool:
        await self.connect()
        try:
            await self.client.delete(key)
            return True
        except Exception:
            return False
    
    async def delete_pattern(self, pattern: str) -> int:
        await self.connect()
        keys = await self.client.keys(pattern)
        if keys:
            return await self.client.delete(*keys)
        return 0

_cache = None

def get_cache() -> RedisCache:
    global _cache
    if _cache is None:
        _cache = RedisCache()
    return _cache

def cached(ttl: int = 300, prefix: str = ""):
    def decorator(func):
        @wraps(func)
        async def wrapper(*args, **kwargs):
            cache = get_cache()
            
            # Generate key
            key_data = f"{prefix}:{func.__name__}:{str(args)}{str(kwargs)}"
            import hashlib
            key = hashlib.md5(key_data.encode()).hexdigest()
            
            # Try cache
            cached_value = await cache.get(key)
            if cached_value is not None:
                return cached_value
            
            # Execute and cache
            result = await func(*args, **kwargs)
            await cache.set(key, result, ttl)
            return result
        return wrapper
    return decorator

def invalidate_cache(pattern: str):
    def decorator(func):
        @wraps(func)
        async def wrapper(*args, **kwargs):
            result = await func(*args, **kwargs)
            cache = get_cache()
            await cache.delete_pattern(pattern)
            return result
        return wrapper
    return decorator

# Usage
@cached(ttl=300, prefix="user")
async def get_user_profile(user_id: int):
    await asyncio.sleep(0.5)  # Simulate DB query
    return {"id": user_id, "name": f"User {user_id}"}

@invalidate_cache("user:*")
async def update_user_profile(user_id: int, data):
    await db.update(user_id, data)
    return {"status": "updated"}
```

**39. WebSocket Chat**

```python
from fastapi import WebSocket, WebSocketDisconnect
from typing import Dict, Set, List

class ConnectionManager:
    def __init__(self):
        self.active_connections: Dict[str, Set[WebSocket]] = {}
        self.user_rooms: Dict[int, str] = {}
    
    async def connect(self, websocket: WebSocket, room: str, user_id: int):
        await websocket.accept()
        if room not in self.active_connections:
            self.active_connections[room] = set()
        self.active_connections[room].add(websocket)
        self.user_rooms[user_id] = room
        await self.broadcast_to_room(
            {"type": "user_joined", "user_id": user_id},
            room
        )
    
    def disconnect(self, websocket: WebSocket, room: str, user_id: int):
        if room in self.active_connections:
            self.active_connections[room].discard(websocket)
        if user_id in self.user_rooms:
            del self.user_rooms[user_id]
        # Broadcast leave message
        import asyncio
        asyncio.create_task(self.broadcast_to_room(
            {"type": "user_left", "user_id": user_id},
            room
        ))
    
    async def send_personal_message(self, message: dict, user_id: int):
        room = self.user_rooms.get(user_id)
        if room and room in self.active_connections:
            for websocket in self.active_connections[room]:
                await websocket.send_json(message)
    
    async def broadcast_to_room(self, message: dict, room: str):
        if room in self.active_connections:
            for websocket in self.active_connections[room]:
                try:
                    await websocket.send_json(message)
                except:
                    pass

manager = ConnectionManager()

@router.websocket("/ws/{room}")
async def websocket_endpoint(
    websocket: WebSocket,
    room: str,
    user_id: int  # In production, get from auth
):
    await manager.connect(websocket, room, user_id)
    
    try:
        # Send history
        history = await get_chat_history(room)
        await websocket.send_json({"type": "history", "messages": history})
        
        while True:
            data = await websocket.receive_text()
            message = json.loads(data)
            
            # Store message
            await save_message(room, user_id, message["content"])
            
            # Broadcast
            await manager.broadcast_to_room({
                "type": "message",
                "user_id": user_id,
                "content": message["content"],
                "timestamp": datetime.utcnow().isoformat()
            }, room)
            
    except WebSocketDisconnect:
        manager.disconnect(websocket, room, user_id)
```

**40. Complete CI/CD Pipeline**

```yaml
name: FastAPI CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  lint:
    name: Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          pip install black flake8 isort
      - name: Check formatting
        run: black --check app tests
      - name: Check imports
        run: isort --check-only --profile black app tests
      - name: Run linter
        run: flake8 app tests --max-line-length=100

  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: lint
    
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: test_db
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    
    steps:
      - uses: actions/checkout@v3
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
          cache: 'pip'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt
      - name: Run migrations
        run: alembic upgrade head
        env:
          DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/test_db
      - name: Run tests
        run: pytest --cov=app --cov-report=xml
        env:
          DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/test_db
          SECRET_KEY: test_secret_key_32_chars_long_
          APP_ENV: testing
      - name: Upload coverage
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml

  build:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: read
      packages: write
    
    steps:
      - uses: actions/checkout@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          context: .
          target: production
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest,${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }}

  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/develop'
    environment: staging
    
    steps:
      - uses: actions/checkout@v3
      - name: Configure kubectl
        uses: azure/setup-kubectl@v3
      - name: Set up kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.STAGING_KUBECONFIG }}" > $HOME/.kube/config
      - name: Deploy to staging
        run: |
          kubectl set image deployment/fastapi-app fastapi-app=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }} -n staging
          kubectl rollout status deployment/fastapi-app -n staging

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'
    environment: production
    
    steps:
      - uses: actions/checkout@v3
      - name: Configure kubectl
        uses: azure/setup-kubectl@v3
      - name: Set up kubeconfig
        run: |
          mkdir -p $HOME/.kube
          echo "${{ secrets.PRODUCTION_KUBECONFIG }}" > $HOME/.kube/config
      - name: Deploy to production
        run: |
          kubectl set image deployment/fastapi-app fastapi-app=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ github.sha }} -n production
          kubectl rollout status deployment/fastapi-app -n production
      - name: Verify deployment
        run: |
          curl -f https://api.your-domain.com/health || exit 1
```

---

**End of Quiz & Test Bank**
